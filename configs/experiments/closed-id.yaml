name: closed-id_seed0
regime: closed-id
dataset_manifest: /home/nickyun/links/scratch/new_study/opensemcom/manifests/opensemcom_real.csv
output_dir: /home/nickyun/links/scratch/new_study/opensemcom/runs
samples: 512
calibration_samples: 256
users: 4
seed: 0
model:
  input_dim: 32
  latent_dim: 16
  num_known_classes: 6
  num_unknown_classes: 2
  tasks:
    - classification
    - retrieval
    - detection
    - segmentation
  train_tasks:
    - classification
  train_domains:
    - cifar10
channel:
  kind: awgn
  snr_db: 12.0
